@if(!Request.IsAuthenticated) {
    @*@Html.ActionLink("×¢²á", "Register", "Account") 
    @: |*@ 
    @Html.ActionLink("µÇÂ¼", "Login", "Account")
}
else {
 
    @: »¶Ó­ @Html.ActionLink(User.Identity.Name + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
 
    @: |
    @Html.ActionLink("µÇ³ö", "LogOff", "Account")
}